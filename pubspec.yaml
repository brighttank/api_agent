name: api_agent
description: Generate technology-agnostic api bindings for both client and server.
version: 0.6.1
repository: https://github.com/schultek/api_agent
issue_tracker: https://github.com/schultek/api_agent/issues

dependencies:
  analyzer: ^8.0.0
  build: ^3.0.0
  collection: ^1.17.2 # pinned by flutter
  dart_style: ^3.0.0
  http: ^1.1.0
  path: ^1.8.3
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  source_gen: ^3.0.1
  type_plus: ^2.1.0

dev_dependencies:
  build_runner: ^2.4.6
  lints: ^3.0.0
  test: ^1.24.8

environment:
  sdk: '>=2.12.0 <3.0.0'
